<div class="nav w-100 d-flex justify-content-around" >
  <%= link_to new_plant_path, class: "nav-link" do %>
    <i class="fa-solid fa-magnifying-glass nav-icon"></i>
    <span class="text <%= "fw-bold" if request.original_url.include?("plants/new") %>">Identify</span>
  <% end %>
  <%= link_to diagnose_path, class: "nav-link" do %>
    <i class="fa-solid fa-stethoscope nav-icon"></i>
    <span class="text <%= "fw-bold" if request.original_url.include?("diagnose") %>">Diagnose</span>
  <% end %>
  <%= link_to plants_path, class: "nav-link" do %>
    <i class="fa-solid fa-seedling nav-icon"></i>
    <span class="text <%= "fw-bold" if request.original_url.include?("plants") && request.original_url.exclude?("listing") && request.original_url.exclude?("new") %>">My Garden</span>
  <% end %>
  <%= link_to listings_plants_path, class: "nav-link" do %>
    <i class="fa-solid fa-store nav-icon"></i>
    <span class="text <%= "fw-bold" if request.original_url.include?("listing") || request.original_url.include?("offer")%> ">Marketplace</span>
  <% end %>
</div>
